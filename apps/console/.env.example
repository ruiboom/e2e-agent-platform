# Console env (Next.js loads .env files from this directory).
# Copy to .env.local for local dev.
DATABASE_URL=postgresql://postgres:postgres@localhost:5433/agent_platform
MODEL_ROUTER_URL=http://localhost:8789
GROUND_URL=http://localhost:8790
BUILD_RUNTIME_URL=http://localhost:8791
EVAL_URL=http://localhost:8792
OPTIMISE_URL=http://localhost:8793
SESSION_SECRET=dev-only-change-me

# OIDC SSO (optional). When set, a valid Bearer token is accepted; set
# SESSION_PROVIDER=oidc to disable the dev-stub login in production.
# OIDC_ISSUER=https://your-idp/
# OIDC_AUDIENCE=agent-platform
# OIDC_JWKS_URL=https://your-idp/.well-known/jwks.json
# OIDC_ROLE_CLAIM=role

# Browser-exposed (client-side cost iframe + feedback widget).
NEXT_PUBLIC_COST_TRACKER_URL=http://localhost:8787
NEXT_PUBLIC_FEEDBACK_TRACKER_URL=http://localhost:8788
